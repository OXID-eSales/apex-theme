var paymentInputs = document.querySelectorAll("#payment .form-check input[type=radio]");
if (paymentInputs) {
  paymentInputs.forEach((item) => {
    item.addEventListener("click", function(evt) {
      document.querySelectorAll(".payment-option", evt.target).forEach((item2) => {
        item2.style.display = "none";
      });
      var targetParentDl = evt.target.parentNode.parentNode;
      targetParentDl.querySelectorAll(".payment-option").forEach((item2) => {
        item2.style.display = "block";
      });
    });
  });
}
//# sourceMappingURL=payment.min.js.map
