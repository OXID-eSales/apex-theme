{{ script({ include: "js/pages/details/hoverzoom.min.js", priority: 11, dynamic: __oxid_include_dynamic }) }}

{% set mediaItems = mediaItems|default(oView.getMediaItems()) %}

<div class="carousel-inner">
    {% for mediaItem in mediaItems %}
        <div class="carousel-item{% if loop.first %} active{% endif %}">
            <figure class="zoom-container-hover">
                <img src="{{ mediaItem.getZoomUrl()|raw }}"
                     alt="{{ mediaItem.attributes.has('alt') ? mediaItem.attributes.alt : '' }}">
            </figure>
        </div>
    {% endfor %}
</div>
