{% block details_productmain_gallery_sliders %}
    {% set mediaItems = mediaItems|default(oView.getMediaItems()) %}
    {% set themeZoom = theme.setting('productZoomType') %}
    {% if not themeZoom %}
        {% include "page/details/inc/gallery/zoom/product_no_zoom.html.twig" with { mediaItems: mediaItems } %}
    {% else %}
        {% set filePath = oDetailsProduct.oxarticles__oxtemplate.value %}

        {% if filePath %}
            {% set filename = filePath|split('/')|last %}
            {% set pictureZoomOption = 'product_' ~ filename %}
        {% else %}
            {% set pictureZoomOption = 'product_' ~ themeZoom ~ '.html.twig' %}
        {% endif %}

        {% if pictureZoomOption %}
            {% include "page/details/inc/gallery/zoom/" ~ pictureZoomOption with { mediaItems: mediaItems } %}
        {% else %}
            {% include "page/details/inc/gallery/zoom/product_no_zoom.html.twig" with { mediaItems: mediaItems } %}
        {% endif %}
    {% endif %}
{% endblock %}
